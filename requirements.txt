# BusinessIntelligence.ai — Python dependencies
# Every package here is actually imported somewhere in api/, engine/,
# narrative/, feedback/ or data/ — kept minimal on purpose so a fresh
# `pip install -r requirements.txt` is fast and doesn't drag in unrelated
# toolchains (there is no Streamlit UI, no Supabase, no EconML in this repo).

# In-memory OLAP warehouse — bundles its own parquet reader/writer
duckdb>=0.10.0

# Mathematical & statistical core (LMDI, STL baselines, DiD, isotonic calibration)
# scipy isn't imported directly but is a hard dependency of statsmodels, so pip
# resolves it automatically — no need to pin it here.
numpy>=1.26.0
pandas>=2.2.0
statsmodels>=0.14.0
scikit-learn>=1.5.0

# Semantic contract validation
pydantic>=2.7.0
pyyaml>=6.0.1

# API layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# LLM provider client — used for any OpenAI-compatible endpoint
# (Groq, NVIDIA NIM, OpenRouter, Ollama). Not required if you only
# run with the offline template narrator (no API key configured).
openai>=1.30.0
python-dotenv>=1.0.1

# Testing
pytest>=8.2.0
